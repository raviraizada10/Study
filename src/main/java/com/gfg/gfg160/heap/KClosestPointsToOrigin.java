package com.gfg.gfg160.heap;

public class KClosestPointsToOrigin {
//    public int[][] kClosest(int[][] points, int k) {
//        PriorityQueue<Integer> distanceHeap = new PriorityQueue<>();
//        for (int i = 0; i < points.length; i++) {
//            int distance = Math.sqrt((points[i][0]) * (points[i][0]) + (points[i][1])* (points[i][1]));
//            distanceHeap.offer(distance);
//        }
//    }
}
